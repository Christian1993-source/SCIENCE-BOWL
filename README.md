Place your royalty-free game-show audio files here.

Required files:
- `bgm.mp3` (main loop)
- `buzzer.mp3`
- `correct.mp3`
- `wrong.mp3`
- `timeout.mp3`
- `tick.mp3`

Notes:
- Keep clips short for SFX (about 0.2s to 1.5s).
- `bgm.mp3` should loop cleanly.
- If files are missing, the app now uses built-in synth fallback audio.
